import { IController } from "../../types/IController";
import { Response } from 'express';
const transcoding: IController = async (req, res) => {
  try {
    
  } catch (error) {
    console.log("TRANSCODING CONTROLLER: ", error);
  }
};

export default {
  transcoding,
};
